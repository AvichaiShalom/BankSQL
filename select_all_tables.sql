USE bankdb;

-- שליפת כל הנתונים מטבלת לקוחות
SELECT * FROM Customers;

-- שליפת כל הנתונים מטבלת סניפים
SELECT * FROM Branches;

-- שליפת כל הנתונים מטבלת עובדים
SELECT * FROM Employees;

-- שליפת כל הנתונים מטבלת חשבונות בנק
SELECT * FROM BankAccounts;

-- שליפת כל הנתונים מטבלת כרטיסי אשראי
SELECT * FROM CreditCards;

-- שליפת כל הנתונים מטבלת עסקאות
SELECT * FROM Transactions;

-- שליפת כל הנתונים מטבלת הלוואות
SELECT * FROM Loans;

-- שליפת כל הנתונים מטבלת תשלומים
SELECT * FROM Payments;
